#include<stdio.h>

int main(s)
{
    //declare x
    int x, s, y, a;
    //calculate 
    do 
    {
        printf("enter the integer: ");
        scanf("%d", &x);
    }
    while (x < 0);
    {
    do (int s = 0; s < x; s++)
    {
        int y += s;
    }
    while (s < x);
    }
    {
        printf("%d", y);
    }
    return 0
}


